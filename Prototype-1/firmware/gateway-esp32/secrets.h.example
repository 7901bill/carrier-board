#pragma once

struct WifiCred { const char* ssid; const char* password; };

const WifiCred wifiCreds[] = {
  { "YOUR_WIFI_SSID", "YOUR_WIFI_PASSWORD" },
  // add more networks as needed:
  // { "AnotherNetwork", "another-password" },
};
const int wifiCredCount = sizeof(wifiCreds) / sizeof(wifiCreds[0]);
